generated: '2026-07-21'
method: derived
source: openapi/sendspark-openapi-original.json
authentication:
  style: api-key
  transport: two request headers, both required
  headers:
  - x-api-key
  - x-api-secret
  ref: authentication/sendspark-authentication.yml
  notes: >-
    Workspace-scoped keys. GET /v1/workspaces/identifier resolves the workspace
    id for a given key pair.
idempotency:
  supported: false
  notes: No Idempotency-Key header or idempotency contract is documented in the OpenAPI.
pagination:
  style: offset-limit
  params:
  - offset
  - limit
  notes: offset/limit query parameters appear on list operations in the spec.
versioning:
  scheme: uri-path
  versions:
  - v1
  - v2
  notes: >-
    Path-prefixed versions coexist (/v1/..., /v2/..., /dv-v2/...). Newer campaign
    operations are published under /v2 while /v1 remains available.
error_envelope:
  shape: flat-json
  fields: [statusCode, code, error, message]
  ref: errors/sendspark-problem-types.yml
  rfc9457: false
rate_limit_signaling:
  documented: false
webhooks:
  supported: true
  ref: asyncapi/sendspark-webhooks-asyncapi.yml
  notes: >-
    Outbound webhooks are managed via the API (create/list/get/patch/delete) and
    fire on video lifecycle and engagement events.
